<% btn_type = "btn-success" if booking.status = "accepted" %>
<% btn_type = "btn-danger" if booking.status in %w[rejected cancelled] %>
<% btn_type = "btn-warning" if booking.status = "pending" %>
<button class="btn <%= btn_type %>" disabled><%= booking.status %></button>
